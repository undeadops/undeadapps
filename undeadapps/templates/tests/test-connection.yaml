apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "undeadapps.fullname" . }}-test-connection"
  labels:
    {{- include "undeadapps.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "undeadapps.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
